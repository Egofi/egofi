import type {
  Invoice,
  PaymentInstructions,
  RailEvent,
  RailStatus,
  RailType,
  RouteQuery,
  SettlementRail,
} from "@egofi/types";

export type { Invoice, PaymentInstructions, RailEvent, RailStatus, RailType, RouteQuery };
export type { SettlementRail };

export const SETTLEMENT_RAIL_TOKEN = "SETTLEMENT_RAILS";
